import { RepaintOnScrollDirective } from './repaint-on-scroll.directive';

describe('RepaintOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RepaintOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
